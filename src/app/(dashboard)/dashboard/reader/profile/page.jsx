import ReaderProfileClient from "./ReaderProfileClient";

export const metadata = {
  title: 'Profile Settings — Fable',
};

export default function ReaderProfilePage() {
  return <ReaderProfileClient />;
}
